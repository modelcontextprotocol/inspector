# Composable Test Server Configs

Sample config files for the composable test server (`server-composable --config <path>`).

- **demo.json** — Minimal server with echo tool only (stdio). Use for smoke testing.
