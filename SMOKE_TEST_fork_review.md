# Smoke Test: Fork Review Workflow

This is a throwaway pull request used only to smoke-test the
`claude-fork-review` GitHub Actions job.

That job runs on pull requests from forks when a maintainer applies the
`claude-review` label.

This PR contains no code or scripts. It can be closed and the branch
deleted once the workflow run has been observed.
